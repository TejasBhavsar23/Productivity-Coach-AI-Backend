package com.productivitycoach;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Entry point for the AI Productivity Coach Spring Boot application.
 * Enables auto-configuration, component scanning, and property support.
 */
@SpringBootApplication
public class AiProductivityCoachApplication {

    public static void main(String[] args) {
        SpringApplication.run(AiProductivityCoachApplication.class, args);
    }
}
